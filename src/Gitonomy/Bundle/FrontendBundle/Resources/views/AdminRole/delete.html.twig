{% extends "GitonomyFrontendBundle::layout.html.twig" %}

{% block title 'Delete role '~object.name %}
{% set navigation_active = null %}

{% block content %}
    <h1>Delete role "{{ object.name }}" ?</h1>
    <form class="form-stacked" action="{{ path('gitonomyfrontend_adminrole_delete', {'id':object.id}) }}" method="POST" novalidate>
        {{ form_errors(form) }}
        {{ form_rest(form) }}

        <input type="submit" class="btn danger" value="Delete" />
        <a href="{{ path('gitonomyfrontend_adminrole_edit', {'id':object.id}) }}" class="btn">Cancel</a>
    </form>
{% endblock %}