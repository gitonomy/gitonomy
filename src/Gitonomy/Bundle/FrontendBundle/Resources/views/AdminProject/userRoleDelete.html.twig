{% extends "GitonomyFrontendBundle:AdminProject:layout.html.twig" %}

{% block title 'Delete user role' %}
{% set navigation_active = 'userRoles' %}

{% block content_right %}
    <h2>Remove {{ userRole.user.fullname }} from project {{ object.name }} ?</h2>
    <form class="form-stacked" action="{{ path('gitonomyfrontend_adminproject_userRoleDelete', {'userRoleId':userRole.id}) }}" method="POST" novalidate>
        {{ form_errors(form) }}
        {{ form_rest(form) }}

        <input type="submit" class="btn btn-danger
btn btn-danger" value="Delete" />
        <a href="{{ path('gitonomyfrontend_adminproject_userRoles', {'id':object.id}) }}" class="btn">Cancel</a>
    </form>
{% endblock %}
