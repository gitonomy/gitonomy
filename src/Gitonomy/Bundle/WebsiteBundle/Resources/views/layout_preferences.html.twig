{% extends "GitonomyWebsiteBundle::layout.html.twig" %}

{% set global_navigation = 'preferences' %}

{% block content %}
    <h1 class="with-menu">
        Preferences
    </h1>

    <div id="wrapper">
        <ul id="local-menu">
            <li><a href="{{ path('preferences_informations') }}"{% if local_navigation == 'informations' %} class="active"{% endif %}>Informations</a></li>
            <li><a href="{{ path('preferences_sshKeys') }}"{% if local_navigation == 'sshKeys' %} class="active"{% endif %}>SSH Keys</a></li>
        </ul>

        <div id="content" class="with-menu">
            {% block preferences_content '' %}
        </div>
    </div>
{% endblock %}
