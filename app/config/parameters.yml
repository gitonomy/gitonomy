parameters:
  # Database
  database_driver:   pdo_mysql
  database_host:     localhost
  database_port:     ~
  database_name:     gitonomy
  database_user:     root
  database_password: ~

  # Mailer
  mailer_transport:  smtp
  mailer_host:       localhost
  mailer_user:       ~
  mailer_password:   ~

  # Security
  secret:            ThisTokenIsNotSoSecretChangeIt

  # Localisation
  locale:            en_US

  # System
  repository_path:   %kernel.root_dir%/cache/repositories
  ssh_access:        alex@localhost

  # Your project
  project_name:      gitonomy.sample
  project_baseline:  Git repositories inside infrastructure with a beautiful interface
  open_registration: true
  mail_from:         noreply@example.org
  mail_to:           admin@example.org
